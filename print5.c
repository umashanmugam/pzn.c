#include <stdio.h>
int main()
{
	int i,n,mul;
	printf("Enter the no:");
	scanf("%d",&n);
            for(i = 1 ; i <= 5 ; i++) 
           {
           	mul=i*n;
            printf("\n%d", mul);
           }
 return 0; 
}
