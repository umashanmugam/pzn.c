#include <stdio.h>
int main()
{
	int N;
	scanf("%d",&N);
	{
	           if((N>=1)&&(N<=10))		{
			printf("Yes %d is present",N);
		}
		else
		{
			printf("No %d is not present",N);
		}
	}
	return 0;
	
}
